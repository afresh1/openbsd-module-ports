#!/bin/sh

find . -name 'pkg' | sed -e 's,^./,,;s,/*pkg$,,' | while read d; do
	( [ -e $d/Makefile ] && cd $d && make all-dir-depends test-dir-depends )
done | tsort | while read p; do
	test -e "$p" && echo $p
done
